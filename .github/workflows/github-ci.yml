name: github-ci
on: 
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    - name: Install modules
      run: npm
    - name: Run ESLint
      run: eslint . --ext .js,.jsx,.ts,.tsx